# Source code

`app` contains pages and API routes, `components` contains the user interface, `config` contains acquisition rules, `contracts` contains shared Zod boundaries, and `server` contains server-only auth, persistence, safety, AI, memory, and escalation modules. Shared names and flows follow `TEAM_CONTRACT.md`.
